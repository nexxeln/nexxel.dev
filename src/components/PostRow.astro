---
import type { CollectionEntry } from "astro:content"
import { formatDate } from "../lib/date"
import Link from "./Link.vue"

type Props = {
  post: CollectionEntry<"blog">
}

const { post: { data: { title, date }, slug } } = Astro.props
---

<article class="flex justify-between">
  <Link href={`/blog/${slug}`}>
    <span class="font-medium text-neutral-100">{title.toLowerCase()}</span>
  </Link>

  <span>{formatDate(date).toLowerCase()}</span>
</article>
